# 目录说明

`flutter_client_demo/` 是当前这套 UI 自动化框架的被测 Flutter Demo 客户端。

这个工程包含：

- `lib/`：业务页面与组件
- `test/`：Flutter widget test
- `test_driver/`：当前主用的 Flutter UI 自动化实现
- 各平台壳工程，例如 `ios/`、`android/`、`web/`

当前自动化主链路直接针对这个 Demo 运行。
